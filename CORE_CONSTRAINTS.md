# 项目核心约束

这份文件用于约束 `gps-geotag` 项目的后续实现方向。后续新增功能、重构、自动化或文档更新，都不应违反这里的规则。

## 1. 项目定位

- 项目只聚焦“相机照片同步到电脑后修正 GPS”。
- 项目不是完整 DAM，不负责管理全部摄影资产生命周期。
- 项目不是后期编辑器，不承担 Lightroom、Capture One 一类工具的职责。

## 2. 资产模型

- 同 basename 的配套文件是一个拍摄单元。
- `RAW + JPG` 是进入 GPS 修正流程的最小核心组合。
- `RAW` 是元数据权威源。
- `JPG` 是同步目标，不是独立决策源。
- 如果存在同 basename 的 `XMP`，它也是需要同步维护的 companion file。
- 不允许把 `JPG` 单独当作 GPS 真值来源。

## 3. 时间与轨迹匹配

- 默认信任照片内的 `DateTimeOriginal` 与 `OffsetTimeOriginal`。
- 默认 `geosync=0`，但必须支持显式时间偏移。
- GPX 匹配由 `exiftool` 完成，不在 Go 内自行重写轨迹插值逻辑。
- 多个 `GPX` 文件必须逐个传参，不能拼接成一个无效字符串参数。

## 4. 写入顺序与成功判定

- 必须先写 `RAW`，再同步到 `JPG`。
- 如果存在同 basename 的 `XMP`，必须继续同步 GPS 到 `XMP`。
- 不允许只依据 `exiftool` 退出码判断成功。
- 成功判定必须包含二次校验，至少确认 `RAW` 上存在有效 `GPSPosition`。
- `JPG` 和 `XMP` 同步后也必须做结果校验。

## 5. 配对与归档

- `RAW` 缺少同名 `JPG` 时，必须等待，不允许提前归档。
- 独立 `JPG` 不处理、不归档、不反向推断 `RAW`。
- 如果照片时间未落在当前 `GPX` 轨迹范围内，整组文件必须继续保留在 `Inbox/`。
- 成功处理后，必须把同 basename 的整组 companion files 一起归档。
- 典型 companion files 包括 `XMP`、`ACR`、`WAV` 等。
- 归档目录固定为 `Processed/YYYY/MMDD/`。
- 归档基于原始拍摄日期，不基于处理日期。

## 6. 并发与待处理清单

- `Inbox` 必须支持按拍摄单元并发处理。
- 并发粒度是 basename 资产组，不允许把同一组文件拆散并发。
- 对于留在 `Inbox` 的待处理资产，必须生成一份用户可读的原因清单。
- 原因清单至少包含：失败原因、建议动作、涉及文件、拍摄时间、时区、参与匹配的 GPX。

## 7. 用户可见输出

- 所有用户可见日志、终端输出、提示文案必须使用中文。
- 文件扩展名、命令名、Exif 字段名可以保留英文原词。
- 错误信息要让用户看得懂，至少能区分：
  - 缺少 GPX
  - 缺少配对 JPG
  - 轨迹时间不匹配
  - GPS 未实际写入
  - 外部命令执行失败

## 8. 工程实现约束

- 当前主实现语言是 Go。
- 当前底层元数据执行器是 `exiftool`。
- 统一使用 `photools` 命令行工具作为系统入口，不再分散定义零散的二进制程序。
- 除非有非常明确的收益，否则不要为了兼容历史习惯保留冗余 shell 包装层。
- 新增功能时优先保持 CLI 简洁和可测试，而不是先做常驻守护或复杂后台系统。
- 允许新增一个薄 Mac 原生图形工作台，但只能作为 `photools` 的可视化控制台和结果查看入口，不允许绕过 CLI 重写 GPS 写入、配对、同步或归档规则。

## 9. 非目标

以下方向默认不在当前项目目标内：

- 云同步或远程服务化
- 完整 DAM 或照片管理型图形界面
- 照片筛选、打分、标签管理
- 照片预览、后期编辑或调色工作流
- 独立于 `exiftool` 的 EXIF 写入实现
